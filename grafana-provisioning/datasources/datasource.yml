apiVersion: 1
datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: db:5432
    user: kjune0922
    password: dlrudalswns2
    database: kuber_db
    jsonData:
      sslmode: "disable"
    isDefault: true
